{
  "version": "0.2.16",
  "message": "fix: scoped /settings model list (global vs session) with inline [+add global] / [+add local] buttons and a unified Up/Down navigation chain; fix /agents session-scoped model routing so a per-session agent model actually routes to that model instead of silently falling back to Main (re-overlay session_models at delegation); per-session toast when an agent's declared model can't resolve; serde hardening on the settings IPC snapshot."
}
