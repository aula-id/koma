.serena/
.env
**/*.pyc
.agents/

# build artifacts — non-anchored so they match at ANY depth
# (root-anchored /target missed src-agent/target etc.)
target/
node_modules/
dist/

# Python virtual environments
.venv/

# model caches (downloaded at runtime)
.fastembed_cache/

# nix build output symlinks
result
result-*
# browser screenshots (transient)
.screenshoot/
.problems/
