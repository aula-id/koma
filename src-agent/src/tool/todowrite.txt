You have the ability to create and maintain a task list (todo list) for your current session. This helps you track your progress, organize complex tasks, and ensure nothing is forgotten.

## When to Use
Use the todowrite tool when:
- Starting a complex multi-step task to break it down
- You realize you need to track several items during your work
- Updating the status of items as you progress

## Format
Each todo item has:
- **content**: A brief description of the task
- **status**: One of `pending`, `in_progress`, `completed`, `cancelled`
- **priority**: One of `high`, `medium`, `low`

## Rules
- Always provide the COMPLETE updated list when called (not just changed items)
- Mark items as `in_progress` when actively working on them
- Mark items as `completed` when finished
- Mark items as `cancelled` if they become irrelevant
- Keep descriptions concise (under 80 characters)
- Use `high` priority for critical path items, `medium` for standard work, `low` for nice-to-haves
